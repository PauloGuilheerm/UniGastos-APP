import { ViewStyle, TextStyle } from 'react-native';

export interface ChatMessageStyle {
    container: ViewStyle;
    totalContainer: ViewStyle;
    spendContainer: ViewStyle;
    text: TextStyle;
    chatInputSend: ViewStyle;
    chatEmpty: ViewStyle;
    chatEmptyText: TextStyle;
  }