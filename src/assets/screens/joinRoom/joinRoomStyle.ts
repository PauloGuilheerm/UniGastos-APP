import {  ViewStyle, TextStyle } from 'react-native';

export interface joinRoomStyle {
  container: ViewStyle;
  inputContainer: ViewStyle;
  iconContainer: ViewStyle;
  joinRoomButton: ViewStyle;
  joinButtonContainer: ViewStyle;
}

export interface joinRoomHeaderStyle {
  container: ViewStyle;
  text: TextStyle;
}